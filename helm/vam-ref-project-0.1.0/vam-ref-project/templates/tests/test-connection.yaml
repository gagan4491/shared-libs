apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vam-ref-project.fullname" . }}-test-connection"
  labels:
    {{- include "vam-ref-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vam-ref-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
