apiVersion: v1
kind: Service
metadata:
  name: {{ include "vam-helm-project.fullname" . }}
  labels:
    {{- include "vam-helm-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vam-helm-project.selectorLabels" . | nindent 4 }}
