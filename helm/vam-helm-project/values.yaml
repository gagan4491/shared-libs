# Default values for vam-refrence-project.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
nameSpace: web-dev
pullSecretName: wubs-deployer

imageUrl: artifactory.wureachprod.awswuintranet.net/wu-businesssolutions-wubs-docker-dev/vam-ref-project/vam-ref-project:210314-064143-1dd7a83

image:
  repository: artifactory.wureachprod.awswuintranet.net/wu-businesssolutions-wubs-docker-dev/vam-ref-project/vam-ref-project
  pullPolicy: IfNotPresent

imagePullSecrets: 
 - name: wubs-deployer
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: NodePort
  port: 80

ingress:
  enabled: true
  annotations: 
  # nginx.ingress.kubernetes.io/force-ssl-redirect: 'true'
  # nginx.ingress.kubernetes.io/rewrite-target: "/"
  # nginx.ingress.kubernetes.io/ssl-redirect: 'true'

  hosts:
    - host: a0b09e56ec205443a86b827df83f1f49-47ed3e5286c84881.elb.us-east-1.amazonaws.com
      paths: 
      - backend:
          serviceName: vam-refrence-project
          servicePort: 80
        path: /
  tls:
  #- hosts:
  # - aa2c11b8585d749d4ba4a3e752d6694b-0e02eb09d277998c.elb.us-east-1.amazonaws.com
  #secretName: neuron-dev-tls-v000
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
