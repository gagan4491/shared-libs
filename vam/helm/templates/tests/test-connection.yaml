apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vam-refrence-project.fullname" . }}-test-connection"
  labels:
    {{- include "vam-refrence-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vam-refrence-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
