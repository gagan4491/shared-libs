apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vam.fullname" . }}-test-connection"
  labels:
    {{- include "vam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget

      image: busybox
      command: ['wget']
      args: ['{{ include "vam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
